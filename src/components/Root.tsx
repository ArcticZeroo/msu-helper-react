import * as React from 'react';
import PortalPage from './navigation/PortalPage';

const Root = (props = null) => (
    <div className="App">
        <PortalPage />
    </div>
);

export default Root;